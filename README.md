# tabplotlib

A very simple "wrapper" for organizing matplotlib plots into tabs. Currently only works for `tk` backend.

# Installation

Available on PyPI, install via pip or whatever you prefer:

``` bash
pip install tabplotlib
```

# Usage

Using this is rather simple, view `examples/` 
